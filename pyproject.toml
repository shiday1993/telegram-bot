[project]
name = "telegram-notifier"
version = "0.1.0"
description = "Telegram notification service"
requires-python = ">=3.13"

dependencies = [
    "fastapi",
    "httpx",
    "python-dotenv",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]